# Loom backend
APP_NAME=Loom
APP_ENV=development

# Reserved for LLM-backed agents in later steps.
# The current lore_agent uses heuristic extraction and does not require a key.
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o-mini
